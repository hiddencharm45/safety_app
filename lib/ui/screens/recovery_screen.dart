import 'package:flutter/material.dart';

class RecoveryPage extends StatelessWidget {
  static const routeName = '/recovery-page';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        //recovery screen code will be here
        );
  }
}
